const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open')});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle?.setAttribute('aria-expanded','false')}));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const feedbackForm=document.querySelector('#feedback-form');feedbackForm?.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(feedbackForm);const lines=[`Name: ${data.get('name')||'Not provided'}`,`Email: ${data.get('email')||'Not provided'}`,`State: ${data.get('state')||'Not provided'}`,`Current journal: ${data.get('currentTool')||'Not provided'}`,`Feedback type: ${data.get('feedbackType')||'General feedback'}`,`Quote permission: ${data.get('quotePermission')?'Yes':'No'}`,'',String(data.get('message')||'')];const subject=encodeURIComponent('NotaryJotter feedback');const body=encodeURIComponent(lines.join('\n'));const note=document.querySelector('#feedback-note');if(note)note.textContent='Opening your email app with this feedback.';window.location.href=`mailto:support@notaryjotter.com?subject=${subject}&body=${body}`});
